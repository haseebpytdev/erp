import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';

const source = fs.readFileSync(new URL('../../public/erp-theme/js/dedicated-product-navigation.js', import.meta.url), 'utf8');
const tick = () => new Promise(resolve => setImmediate(resolve));

class Node {
  constructor(tag = 'div') { this.tagName = tag.toUpperCase(); this.children = []; this.parentNode = null; this.attrs = {}; this.classList = { add: (...names) => { const classes = this._classes(); names.forEach(name => classes.add(name)); this.attrs.class = [...classes].join(' '); }, remove: (...names) => { const classes = this._classes(); names.forEach(name => classes.delete(name)); this.attrs.class = [...classes].join(' '); }, toggle: (name, force) => { const classes = this._classes(); const next = force === undefined ? !classes.has(name) : !!force; if(next) classes.add(name); else classes.delete(name); this.attrs.class = [...classes].join(' '); return next; }, contains: name => this._classes().has(name) }; this.dataset = new Proxy({}, { set: (target, key, value) => { target[key] = String(value); this.attrs['data-' + String(key).replace(/[A-Z]/g, c => '-' + c.toLowerCase())] = String(value); return true; } }); this.listeners = {}; this.href = ''; this.target = ''; this.rel = ''; this.src = ''; this._onload = null; Object.defineProperty(this, 'onload', { get: () => this._onload, set: fn => { const prior = this._onload; this._onload = () => { if (prior) prior(); if (fn) fn(); }; } }); }
  _classes() { return new Set(String(this.attrs.class || '').split(/\s+/).filter(Boolean)); }
  appendChild(node) { node.parentNode = this; this.children.push(node); if (node._onload) node._onload(); return node; }
  replaceChild(node, oldNode) { const index = this.children.indexOf(oldNode); if (index < 0) return oldNode; node.parentNode = this; this.children[index] = node; oldNode.parentNode = null; if (node._onload) node._onload(); return oldNode; }
  remove() { if (this.parentNode) { this.parentNode.children = this.parentNode.children.filter(child => child !== this); this.parentNode = null; } }
  set innerHTML(value) { this.children.forEach(child => { child.parentNode = null; }); this.children = []; }
  get innerHTML() { return ''; }
  setAttribute(key, value) { this.attrs[key] = String(value); if (key === 'href') this.href = String(value); if (key === 'src') this.src = String(value); if (key === 'target') this.target = String(value); if (key === 'rel') this.rel = String(value); if (key.startsWith('data-')) this.dataset[key.slice(5).replace(/-([a-z])/g, (_, c) => c.toUpperCase())] = String(value); }
  removeAttribute(key) { delete this.attrs[key]; }
  getAttribute(key) { return this.attrs[key] ?? null; }
  hasAttribute(key) { return this.getAttribute(key) !== null; }
  addEventListener(type, fn) { (this.listeners[type] ??= []).push(fn); }
  dispatchEvent(event) { for (const fn of this.listeners[event.type] || []) fn.call(this, event); }
  closest(selector) { let node = this; while (node) { if (node.matches(selector)) return node; node = node.parentNode; } return null; }
  matches(selector) { const attr = selector.match(/^([\w-]+)?\[([^=\]]+)(?:="?([^\]"]+)"?)?\]$/); if (attr) { const actual = this.getAttribute(attr[2]) ?? this[attr[2]] ?? null; return (!attr[1] || this.tagName === attr[1].toUpperCase()) && actual !== null && (attr[3] === undefined || actual === attr[3]); } return this.tagName === selector.toUpperCase(); }
  querySelectorAll(selector) { const result = []; const walk = node => { for (const child of node.children) { if (child.matches(selector)) result.push(child); walk(child); } }; walk(this); return result; }
  querySelector(selector) { return this.querySelectorAll(selector)[0] || null; }
}

function fragment(mode, bookingId) {
  if (mode === 'invalid') return { querySelectorAll: () => [], querySelector: () => null };
  const roots = [];
  const root = id => { const r = new Node('main'); r.setAttribute('data-etgp-dedicated-product', '1'); r.setAttribute('data-etgp-product-key', 'air'); r.setAttribute('data-booking-id', id); const host = new Node(); host.setAttribute('data-etgp-dedicated-product-host', '1'); const body = new Node(); body.setAttribute('data-etgp-dedicated-product-body', '1'); host.appendChild(body); r.appendChild(host); const review = new Node('a'); review.setAttribute('data-et-air-review-action', '1'); r.appendChild(review); return r; };
  roots.push(root(mode === 'mismatch' ? '999' : bookingId)); if (mode === 'multiple') roots.push(root(bookingId));
  return { querySelectorAll: s => s === '[data-etgp-dedicated-product="1"]' ? roots : [], querySelector: s => s === '[data-etgp-dedicated-product="1"]' ? roots[0] : null };
}

function harness(options = {}) {
  const booking = String(options.booking || '31'); const html = new Node('html'); const body = new Node('body'); const head = new Node('head'); const main = new Node('main'); const launcher = new Node('section'); launcher.setAttribute('data-et-booking-products-launcher', '1'); const links = {};
  for (const [name, path] of [['air', 'air'], ['hotel', 'hotel'], ['transport', 'transport'], ['visa', 'visa'], ['other', 'other-services']]) { const a = new Node('a'); a.setAttribute('href', `/operations/bookings/${booking}/products/${path}`); links[name] = a; launcher.appendChild(a); }
  ['et-general-progressive-step1-11390','etgp-step1-live-11390','etgp-step1-ready-11390','etgp-step1-fallback-11390'].forEach(name => html.classList.add(name)); if (options.productPrepaint) html.classList.add('et-booking-products-prepaint'); const generalCss = new Node('link'); generalCss.rel = 'stylesheet'; generalCss.setAttribute('data-et-general-progressive-css', 'TEST'); generalCss.href = '/system/erp-assets/general-progressive-step1.css'; generalCss.disabled = !!options.generalCssDisabled; head.appendChild(generalCss); const review = new Node('a'); review.setAttribute('data-et-booking-review-entry', '1'); body.appendChild(review); const sidebar = new Node('aside'); sidebar.setAttribute('data-sidebar-sentinel', '1'); body.appendChild(sidebar); const outer = new Node(); outer.setAttribute('data-outer-shell-sentinel', '1'); body.appendChild(outer); main.appendChild(launcher); body.appendChild(main); html.appendChild(body); const doc = new Node('#document'); doc.documentElement = html; doc.head = head; doc.body = body; doc.appendChild(head); doc.appendChild(html);
  if (options.preloaded) { const core = new Node('script'); core.setAttribute('data-et-fast-nav-asset', 'dedicated-product-core'); head.appendChild(core); const air = new Node('script'); air.setAttribute('data-et-fast-nav-asset', 'products-air'); head.appendChild(air); const css = new Node('link'); css.setAttribute('data-et-fast-nav-asset', 'dedicated-air-css'); css.setAttribute('rel', 'stylesheet'); head.appendChild(css); }
  const fetches = [], assets = [], pushed = [], assigned = [], replaced = [], popstates = [], order = []; let mounts = 0; let mountSawProductPrepaint = false; let state = { saveInFlight: false, dirty: false, draftPending: false }; const mountResult = options.mountResult ?? true;
  const api = options.preloaded ? { getState: () => state, mount: root => { mounts++; order.push('air'); mountSawProductPrepaint = html.classList.contains('et-booking-products-prepaint'); replaced.push(root); return mountResult; } } : undefined;
  const presentation = []; const location = { origin: 'https://erp.test', href: `https://erp.test/operations/bookings/${booking}`, assign: u => assigned.push(u) }; const window = { location, etDedicatedAirProduct: api, etDedicatedProductCore: options.preloaded ? {} : undefined, etBookingFocus: { mountPresentation: () => { presentation.push('presentation'); order.push('presentation'); return options.presentationResult ?? true; } }, history: { state: null, pushState: (_, __, u) => pushed.push(u), replaceState: (_, __, u) => pushed.push(`replace:${u}`) }, confirm: () => true, addEventListener: (t, fn) => { if (t === 'popstate') popstates.push(fn); } };
  const document = { ...doc, head, documentElement: html, querySelector: s => { if (s === '[data-et-booking-products-launcher="1"]') return launcher; if (s === 'script[data-et-dedicated-product-navigation]') { const n = new Node('script'); n.setAttribute('data-et-dedicated-product-navigation', options.version || 'v1.1.33.TEST'); return n; } if (options.preloaded && s === 'link[data-et-fast-nav-asset="dedicated-air-css"]') return head.querySelector(s); return doc.querySelector(s); }, querySelectorAll: s => doc.querySelectorAll(s), createElement: type => { const n = new Node(type); if (type === 'link' || type === 'script') { n.onload = () => { assets.push({ type, marker: n.getAttribute('data-et-fast-nav-asset'), href: n.href, src: n.src }); if (type === 'script' && n.getAttribute('data-et-fast-nav-asset') === 'dedicated-product-core') window.etDedicatedProductCore = {}; if (type === 'script' && n.getAttribute('data-et-fast-nav-asset') === 'products-air') window.etDedicatedAirProduct = { getState: () => state, mount: root => { mounts++; order.push('air'); mountSawProductPrepaint = html.classList.contains('et-booking-products-prepaint'); replaced.push(root); return mountResult; } }; }; } return n; }, importNode: n => n };
  window.document = document; const mode = options.mode || 'valid'; const ctx = vm.createContext({ window, document, DOMParser: class { parseFromString() { return fragment(mode, booking); } }, URL, AbortController, fetch: (url, opts) => { fetches.push({ url, opts }); return Promise.resolve({ ok: options.fetchOk !== false, text: async () => '<fragment />' }); }, Promise, Array, String, Number, Error, encodeURIComponent, setTimeout, clearTimeout });
  return { ctx, page: { html, body, head, main, launcher, review, sidebar, outer, generalCss }, links, fetches, assets, pushed, assigned, replaced, popstates, presentation, order, mounts: () => mounts, mountSawProductPrepaint: () => mountSawProductPrepaint, run: () => vm.runInContext(source, ctx), setState: v => { state = v; } };
}

async function click(h, link, extra = {}) { const e = { type: 'click', button: 0, defaultPrevented: false, metaKey: false, ctrlKey: false, shiftKey: false, altKey: false, prevented: false, preventDefault() { this.prevented = true; }, ...extra }; link.dispatchEvent(e); await tick(); await tick(); return e; }
async function navigate(h) { h.run(); return click(h, h.links.air); }
let n = 0; const eq = (a, b) => { assert.deepEqual(a, b); n++; }; const yes = value => { assert.ok(value); n++; };

let h = harness(); const bodyBefore = h.page.body; const headBefore = h.page.head; const documentBefore = h.ctx.window.document; eq(h.page.html.classList.contains('et-booking-products-prepaint'), false); const primaryEvent = await navigate(h); yes(primaryEvent.prevented); eq(h.fetches[0].url, '/operations/bookings/31/products/air/fragment'); eq(h.fetches[0].opts.credentials, 'same-origin'); eq(h.fetches[0].opts.headers.Accept, 'text/html'); eq(h.fetches[0].opts.headers['X-Requested-With'], 'XMLHttpRequest'); eq(h.pushed[0], '/operations/bookings/31/products/air'); eq(h.replaced.length, 1); eq(h.mountSawProductPrepaint(), true); eq(h.page.html.classList.contains('et-booking-products-prepaint'), true); yes(h.page.html.classList.contains('et-general-progressive-step1-11390') === false); yes(h.page.html.classList.contains('etgp-step1-live-11390') === false); yes(h.page.html.classList.contains('etgp-step1-ready-11390') === false); yes(h.page.html.classList.contains('etgp-step1-fallback-11390') === false); eq(h.page.generalCss.disabled, true); eq(h.presentation.length, 1); eq(h.order.join(','), 'presentation,air'); yes(h.page.sidebar.parentNode === h.page.body); yes(h.page.outer.parentNode === h.page.body); eq(h.page.review.parentNode, null); yes(h.ctx.window.document.querySelector('[data-etgp-dedicated-product="1"]')); yes(h.ctx.window.document.querySelector('[data-et-air-review-action="1"]')); yes(h.ctx.window.document === documentBefore); yes(h.page.body === bodyBefore); yes(h.page.head === headBefore);
for (const event of [{ ctrlKey: true }, { metaKey: true }, { shiftKey: true }, { altKey: true }, { button: 1 }]) { h = harness(); h.run(); const e = await click(h, h.links.air, event); eq(e.prevented, false); eq(h.fetches.length, 0); eq(h.assigned.length, 0); }
for (const name of ['hotel', 'transport', 'visa', 'other']) { h = harness(); const hrefBefore = h.links[name].href; h.run(); const e = await click(h, h.links[name]); eq(e.prevented, false); eq(h.fetches.length, 0); eq(h.assigned.length, 0); eq(h.links[name].href, hrefBefore); }
for (const mode of ['invalid', 'multiple', 'mismatch']) { h = harness({ mode }); h.run(); await click(h, h.links.air); eq(h.pushed.length, 0); eq(h.assigned[0], '/operations/bookings/31/products/air'); yes(h.page.html.classList.contains('et-booking-products-prepaint') === false); yes(h.page.launcher.parentNode === h.page.main); yes(h.page.review.parentNode === h.page.body); eq(h.mounts(), 0); }
h = harness({ mountResult: false }); await navigate(h); eq(h.pushed.length, 0); eq(h.assigned[0], '/operations/bookings/31/products/air'); eq(h.page.html.classList.contains('et-booking-products-prepaint'), false);
h = harness({ presentationResult: false }); await navigate(h); eq(h.pushed.length, 0); eq(h.assigned[0], '/operations/bookings/31/products/air'); eq(h.page.html.classList.contains('et-general-progressive-step1-11390'), true); eq(h.page.generalCss.disabled, false);
h = harness({ generalCssDisabled: true, mode: 'invalid' }); await navigate(h); eq(h.page.generalCss.disabled, true); eq(h.page.html.classList.contains('et-general-progressive-step1-11390'), true);
h = harness({ productPrepaint: true, mode: 'invalid' }); await navigate(h); eq(h.assigned[0], '/operations/bookings/31/products/air'); eq(h.page.html.classList.contains('et-booking-products-prepaint'), true);
h = harness({ version: 'v1.1.33.TEST' }); await navigate(h); eq(h.assets.map(a => a.type), ['link', 'script', 'script']); for (const a of h.assets) { const url = a.href || a.src; yes(url.includes('v=v1.1.33.TEST')); yes(!url.includes('general-progressive-step1')); } eq(h.assets[1].src, '/system/erp-assets/dedicated-product-core.js?v=v1.1.33.TEST'); eq(h.assets[2].src, '/system/erp-assets/products-air.js?v=v1.1.33.TEST');
h = harness({ preloaded: true }); await navigate(h); eq(h.assets.length, 0); eq(h.mounts(), 1); eq(h.page.head.querySelectorAll('link[data-et-fast-nav-asset="dedicated-air-css"]').length, 1); eq(h.page.head.querySelectorAll('script[data-et-fast-nav-asset="dedicated-product-core"]').length, 1); eq(h.page.head.querySelectorAll('script[data-et-fast-nav-asset="products-air"]').length, 1);
h = harness(); await navigate(h); h.setState({ dirty: true, draftPending: true, saveInFlight: false }); h.ctx.window.location.href = 'https://erp.test/operations/bookings/31/products/hotel'; h.ctx.window.confirm = () => false; h.popstates[0](); eq(h.pushed.at(-1), 'replace:/operations/bookings/31/products/air'); eq(h.assigned.length, 0); eq(h.page.html.classList.contains('et-booking-products-prepaint'), true); yes(h.ctx.window.document.querySelector('[data-etgp-dedicated-product="1"]'));
h = harness(); await navigate(h); h.setState({ dirty: false, draftPending: false, saveInFlight: true }); h.ctx.window.location.href = 'https://erp.test/operations/bookings/31/products/hotel'; h.popstates[0](); eq(h.pushed.at(-1), 'replace:/operations/bookings/31/products/air'); eq(h.assigned.length, 0); eq(h.page.html.classList.contains('et-booking-products-prepaint'), true); yes(h.ctx.window.document.querySelector('[data-etgp-dedicated-product="1"]'));
h = harness(); await navigate(h); h.setState({ dirty: true, draftPending: true, saveInFlight: false }); h.ctx.window.confirm = () => true; h.ctx.window.location.href = 'https://erp.test/operations/bookings/31/products/hotel'; h.popstates[0](); eq(h.assigned[0], 'https://erp.test/operations/bookings/31/products/hotel');
h = harness(); await navigate(h); h.setState({ dirty: false, draftPending: false, saveInFlight: false }); h.ctx.window.location.href = 'https://erp.test/operations/bookings/31/products/hotel'; h.popstates[0](); eq(h.assigned[0], 'https://erp.test/operations/bookings/31/products/hotel');
console.log(`PASS ${n} Air fast-navigation behavioral assertions`);
